---
trigger: always_on
description: エージェント用ルール
---

## 1. 核心的動作原理 (Single Source of Truth)

- **SSoT の遵守**: 実装・設計の判断は、常に `docs/REQUIREMENTS.md`（要件）と `docs/DESIGN.md`（デザイン）を絶対的な正解として扱うこと。
- **セットアップ・構造**: 環境構築の手順やプロジェクトのディレクトリ構成については、ルートの `README.md` を参照すること。
- **勝手な改変の禁止**: ユーザーからの明示的な命令、または `notify_user` による合意なしに、設計書に反する実装を行わないこと。
- **コンテキストの優先順位**: 1. ユーザーの最新の指示, 2. 本ルールファイル, 3. `docs/` 配下のドキュメント。

## 2. 出力・コミュニケーション規約

- **使用言語**: すべての報告、計画、コミットメッセージは**日本語**で生成すること。
- **透明性の確保**: 重要な技術選定（ライブラリの追加など）が発生する際は、`notify_user` を用いて承認を得てから実行すること。

## 3. 開発運用ルール

- **Issue ベースの実行**: タスク開始前に必ず関連する GitHub Issue を確認し、進捗を `task.md` に反映させること。ただし、タイポの修正や軽微な UI 調整などの小規模な修正については、Issue を作成せずに直接 Pull Request（またはブランチ作成）を行って良い。
- **テンプレートの活用**: Issue や Pull Request を作成する際は、`.github/` 配下に定義されたテンプレートを必ず使用し、構造化された情報を記述すること。
- **Git 運用**:
  - **ブランチ戦略**: 新しい機能の開発や修正を行う際は、必ず最新の `main` ブランチから新しいブランチを作成して作業すること。ブランチ名は原則として Issue と対応させること（例: `feat/issue-1-design-system`）。Issue がない小規模な修正の場合は `fix/minor-update` や `docs/update-readme` などの分かりやすい名前を使用すること。
  - **コミットメッセージ**: 日本語で内容を簡潔に記述すること。
  - \*\*リモート同期\*\*: 意味のある単位で `git push` し、作業ブランチをリモートと同期させること。
